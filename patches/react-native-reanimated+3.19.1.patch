diff --git a/node_modules/react-native-reanimated/src/component/Image.ts b/node_modules/react-native-reanimated/src/component/Image.ts
index 1234567..abcdefg 100644
--- a/node_modules/react-native-reanimated/src/component/Image.ts
+++ b/node_modules/react-native-reanimated/src/component/Image.ts
@@ -1,5 +1,6 @@
 'use strict';
-import { Image } from 'react-native';
+import { Image, ImageProps } from 'react-native';
+import React from 'react';
 
 import { createAnimatedComponent } from '../createAnimatedComponent';
 
@@ -9,6 +10,13 @@ interface AnimatedImageComplement extends Image {
   getNode(): Image;
 }
 
-export const AnimatedImage = createAnimatedComponent(Image);
+// Wrapper class component to make Image compatible with createAnimatedComponent
+// This fixes the issue where createAnimatedComponent doesn't support functional components
+class ImageWrapper extends React.Component<ImageProps> {
+  render() {
+    return React.createElement(Image, this.props);
+  }
+}
+
+export const AnimatedImage = createAnimatedComponent(ImageWrapper);
 
 export type AnimatedImage = typeof AnimatedImage & AnimatedImageComplement;